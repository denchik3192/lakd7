import React from 'react';
import { MapContainer, Marker, Popup, TileLayer, useMap } from 'react-leaflet';

const Map: React.FC = () => {
  return (
    <>
      <div>4</div>
    </>
  );
};

export default Map;
